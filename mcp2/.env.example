# Email Provider Configuration
EMAIL_PROVIDER=gmail  # or "outlook"

# Gmail Configuration (if using Gmail)
GMAIL_CREDENTIALS_PATH=gmail_credentials.json

# Outlook Configuration (if using Outlook)
OUTLOOK_CREDENTIALS_PATH=outlook_credentials.json

# Optional: Client tracking database
# DATABASE_URL=sqlite:///client_tracking.db